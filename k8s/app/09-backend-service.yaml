apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: realidadstore
  labels:
    app: backend
    app.kubernetes.io/name: realidadstore
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
